{{ if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: custom-podinfo-read-secrets 
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - get
  - list
{{ end }}

